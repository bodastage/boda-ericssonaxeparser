package com.bodastage;

import org.scalatest._

import scala.io.Source
import java.io.File

class EricssonAXEParserSpec extends FunSuite {
    test("EricssonAXEParser"){
		/*
        val resourcePath = getClass.getResource("/RLDEP.NODENAME")
        var inputFile : String = resourcePath.getPath
        val outDir = System.getProperty("java.io.tmpdir")

        var parser = EricssonAXEParser;
        var args : Array[String] = Array("-i", inputFile, "-o", outDir);
        parser.main(args);

        val expectedCSV = "";

        val sourceCSV = Source.fromFile(outDir + File.separator + "RLDEP.NODENAME").getLines().mkString("\n");
		*/
        assert(true)
    }
}
